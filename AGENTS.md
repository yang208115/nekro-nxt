# NekroNXT 开发约定

本文件保存每次开发都需要的项目边界。详细契约按任务查阅，不要求先通读文档或绘制全仓地图。

## 项目与开发

- 产品名 `NekroNXT`，受限空间简称 `NXT`，根包与仓库名 `nekro-nxt`。这是以 DSH 为核心引擎、原生群聊、兼具娱乐与生产力、可由智能体参与扩展开发的聊天系统。
- TypeScript 主栈、ESM 优先、pnpm workspace 单仓库；Desktop、Server、Web 与共享包共用领域实现。常用命令是 `pnpm dev`、`pnpm check`、`pnpm test`、`pnpm build`。
- 默认直接在 `main` 开发和提交；只有用户明确要求时才创建分支。保留用户已有改动，不在子目录创建嵌套 Git 仓库。
- 提交使用英文类型与中文主题：`type(scope): 中文动词短语`，类型为 `feat`、`fix`、`refactor`、`docs`、`test`、`merge`。
- 抽象、包和配置必须有当前所有者与消费者；成熟、低风险的主流依赖可直接用于已授权需求，重要选型写入对应契约。不为未来可能复用提前创建空层。

## 产品边界

- 面向用户的实体统一称“智能体”。内部 TypeScript 可用 `AgentDefinition` 等名称；“助手”只作具体名称或人设，外部发送账号可称“机器人账号”。平台名来自 Adapter 的 `displayName`，频道来源使用连接展示名，不在通用 UI 写死具体平台。
- 用户显式创建智能体。一个智能体可绑定多个频道，一个频道同一时间只有一个响应智能体；频道消息事实与短期上下文保持隔离。
- 智能体的用户可见发言只由通信工具产生；DSH 原始 `text/reasoning` 只作内部轨迹。管理员从客户端发送的消息是独立事实，不记作模型发送。Mention、图片和文件使用结构化内容块。
- 工具期间的新消息可靠入库，在安全间隙注入；普通消息不得隐式取消当前工具。状态只在真实提交点发布，注册可撤销，dispose 等待资源静止。
- 智能体配置和扩展通过不可变版本演进，运行中的会话不在工具执行中热替换。动态运行、保存扩展版本、给智能体启用是独立动作；动态创造、开发 Shell、完整文件访问分别授权。
- DSH 只通过公开 API、Plugin、Service、Tool、Provider、Settings、Scope、Bundle 或 Preset 扩展；不深 Fork、不复制内核；NekroNXT 自身优先以可替换的 Cordis 插件或 Bundle 组合接入。通用 DSH 能力以零 NekroNXT 适配可用为目标，专用元数据增强产品体验；DSH 原生 WebUI、侧栏、Composer 和页面 Slot 不接入。
- Adapter、Tool、Preset、MCP 和 UI Slot 属于统一 Extension 的不同 Contribution。添加连接消费已安装 Adapter 目录与版本化 schema；系统托管 Adapter 不进入用户创建列表。固定装载扩展标记“内置”，用户安装扩展展示真实来源和加载诊断。
- 用户操作说明对象、范围和结果；可点击控件必须产生真实结果。未实现操作通常隐藏；必须展示时禁用并标注“尚未开放”，不长期占据一级导航或主操作。
- 首版优先打通创造、运行、验证、保存和启用的本地闭环；不提前建设社区市场、签名、SBOM 或复杂审批。Nekro Agent 只提供历史经验，不做旧数据迁移、插件兼容或架构复刻。

## 数据、宿主与发布

- 测试、示例、注释和提交截图只用虚构样本。不得提交凭据、真实聊天、个人标识、群标识、平台临时 URL 或真实文件哈希。
- 宿主使用统一数据根：容器为 `/data`，本地由宿主配置；智能体工作区默认 `workspaces/<agentId>/`。新增顶层项必须有唯一所有者与恢复语义，不在局部任务中静默迁移用户数据。
- Desktop 安装即用，不要求预装 Node、pnpm、Python 或 Docker；Server 以单容器、单主要数据根持续运行。两者复用 Core、Web 和扩展；升级可验证、失败可恢复，不在运行容器中 `git pull`。
- 公开品牌母版在 `assets/brand/`，公开构建不依赖本地私有资料；修改品牌同时遵循[品牌规范](docs/BRAND.md)，品牌素材不属于代码许可证。
- 正式发布必须取得用户对当前完整更新内容的明确同意；修改文案后重新审查。此前可准备文案和验证，不能运行 `pnpm release <版本>`、创建正式 Tag 或触发正式发布。唯一正式入口及其他校验见[发布规范](docs/09-正式发布与更新日志规范.md)。

## 任务范围与完成

- 在已授权范围内完成实现、相关验证和失败修复，无需为普通可逆步骤反复确认；只读任务不修改文件。修复本次造成或阻塞本次正确交付的问题；独立历史问题记录并报告，不自动扩成无关重构。
- 修改行为时同步更新该事实的唯一现行来源；只改变实现、不改变已记载行为时无需制造文档改动。Decision 保存决策理由、状态和契约入口，不凭状态标签覆盖用户决定或当前事实。
- 按风险运行足够的相关检查。用户可见 Web 改动须验证生产构建下的受影响旅程；UI 同时审查源码并亲自查看真实画面。运行时错误、空白根节点、关键页面不可见是硬失败；缺少验证不能宣称交付完成。
- 若改动或验证触发已有开发服务重载，确认原宿主下服务与 API 仍可访问；不能只看进程、单测或 Vite 首页。具体探测见[开发规范 §6.1](docs/06-开发与测试规范.md#61-本地开发服务必须仍可访问)。
- 相关检查通过且没有新的修改、失败或未解决风险后停止重复验证。完成说明写清结果、实际验证和剩余问题，不把无关失败算作本次回归，也不隐藏它。

## 按任务查阅

已知局部问题可直接读相关源码、测试和文档章节；不清楚所有者或权威来源时，从[公开索引](docs/README.md)定位。索引帮助发现知识，不是逐级全文预读关卡；已读且未变化的内容无需重复加载。

| 任务                             | 相关入口                                                                                                                                                                             |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| UI、文案、主题、布局             | [术语](docs/01-术语与文案规范.md)、[设计](docs/05-桌面UI与动效规范.md)、[交互模型](docs/NekroNxt界面交互模型.md)的相关章节；改用户路径再查[旅程](docs/NekroNxt产品形态与用户旅程.md) |
| 消息、Channel、Adapter、模型输出 | [消息协议](docs/03-消息内容与投递协议.md)，跨包边界再查[Decision 索引](docs/decisions/README.md)                                                                                     |
| 工程、测试、生命周期、持久化     | [开发与测试规范](docs/06-开发与测试规范.md)相关章节，以及 [Apps](apps/README.md) / [Packages](packages/README.md) 中的所有者                                                         |
| 新架构、公共契约、外部机制       | 本仓类型与测试、对应现行契约；涉及未来边界查[方向](docs/02-未来扩展方向.md)，需要外部证据查[复用指南](docs/07-参考项目复用指南.md)                                                   |
| 本机服务、环境、私有参考         | 存在时查[本地索引](.local/README.md)，只读取本机差异，不另设通用流程                                                                                                                 |
| 公开文档或真实样本脱敏           | [公开边界](docs/00-文档公开边界.md)                                                                                                                                                  |

新增、移动或归档受维护文档时接入最近的领域索引，运行 `pnpm check:docs`。只有追溯决策背景时才进入归档；具体文档职责与冲突处理见[开发规范 §1](docs/06-开发与测试规范.md#1-知识与决策)。
